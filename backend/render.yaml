services:
  - type: web
    name: training-platform-backend
    env: node
    buildCommand: npm ci --include=dev && npm run build && npx prisma db push
    startCommand: npx prisma db push && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
